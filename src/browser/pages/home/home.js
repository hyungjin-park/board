import styled from "styled-components";
import BoardBody from "./boardBody";

const Home = () => {
  return (
    <HomeArea>
      <BoardBody />
    </HomeArea>
  );
};

const HomeArea = styled.div``;
export default Home;
